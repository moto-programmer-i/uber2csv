/**
 * string汎用メソッド集
 */
class StringUtils {
    /**
     * 文字列が空か判定
     * @param {string} str 対象文字列
     * @returns false: 1文字以上の文字列、true：それ以外
     */
    static isEmpty(str) {
        return !str;
    }
}
